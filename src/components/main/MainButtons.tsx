import styles from '@/styles/main/MainButtons.module.scss'

export default function MainButtons() {
    return (
        <div className={styles.container}>
            <button>Perfil</button>
        </div>
    )
}